# 项目交接文档 · 温馨小木屋

> **给 AI 看的**：这份文档记录了这个项目的完整背景。
> 用户开新对话让你继续维护这个网站时，**先读完这份文档**，你就知道全部情况了。
>
> **给用户看的**：如果换了新对话，跟 AI 说一句
> "继续做我的网站，项目在 `D:\Claude code个人作品网站`，先读 PROJECT-NOTES.md" 就行。

最后更新：2026-09-15

---

## 一、项目概况

| 项目 | 内容 |
|---|---|
| 网站名 | 温馨小木屋 |
| 网址 | https://hi3612.github.io |
| 用途 | 站长（hi3612）展示自己用 AI 制作的动画短片 |
| 主要观众 | **站长自己看**（用户明确说过"就我自己看"），其次才是分享给朋友 |
| GitHub 仓库 | https://github.com/hi3612/hi3612.github.io |
| 本地目录 | `D:\Claude code个人作品网站` |

**用户背景**：非程序员，英文水平有限（**全程用中文交流**，工具调用的 description 也要写中文）。
用户会用 AI 工具（豆包等）生成 2D/3D 动画短片，每集约 1.5 分钟。

---

## 二、技术架构

**纯静态网站，无框架、无构建步骤、无后端。**

```
HTML + 原生 CSS + 原生 JavaScript
    ↓
GitHub Pages 托管（免费）
    ↓
视频文件直接放在仓库里
```

**为什么不用框架（React/Vue 等）**：
- 用户非技术人员，零构建步骤才好维护
- 改一个 JSON 就能加作品
- 加载快、没有依赖

---

## 三、文件结构

```
D:\Claude code个人作品网站\
├── index.html              首页（作品墙 + 继续观看）
├── watch.html              播放页（自定义播放器 + 选集 + 简介）
├── 404.html                404 错误页
├── README.md               给用户看的说明书（怎么加作品、怎么发布）
├── PROJECT-NOTES.md        本文件（给 AI 看的交接文档）
├── 一键发布.bat            ☆ GBK 编码，双击即用
├── 本地预览.bat            ☆ GBK 编码，双击即用
│
├── css/
│   ├── style.css           全站样式（含深色/浅色主题、手机适配）
│   └── player.css          自定义播放器控件样式
│
├── js/
│   ├── data.js             ★ 作品数据（加作品主要改这里）
│   │                         其中 SITE_CONFIG.version 是缓存版本号
│   ├── common.js           公共逻辑（主题切换、本地存储封装）
│   ├── home.js             首页逻辑（作品卡片、统计数字、继续观看）
│   └── watch.js            播放页逻辑（播放器、选集、进度记忆、快捷键）
│
├── assets/covers/          作品封面图
└── videos/<作品id>/        视频文件
```

---

## 四、已实现的功能清单

### 首页
- 作品卡片墙（封面 + 标签 + 集数 + 总时长，悬停有动效）
- 顶部统计（部数 / 集数 / 总分钟数，带数字滚动动画）
- **「继续观看」**：自动找出最近看到一半的那一集（排除已看完的，要求看到 3 秒以上）
- 深色 / 浅色主题切换（记忆用户选择）

### 播放页
- **自定义播放器**（不是浏览器原生控件），木屋暖色调
- **设备自适应交互**：
  - 手机：单击 = 显示/隐藏控制条，双击 = 播放/暂停（带圆形图标反馈）
  - 电脑：单击 = 播放/暂停，双击 = 全屏
- **选集列表**（当前集高亮 + 音波动画，已看完的集有标记）
- **自动连播**（5 秒倒计时，可取消）
- **播放进度记忆**（localStorage，下次接着看）
- **快捷键**：空格暂停、←→ 快退快进、↑↓ 音量、F 全屏、M 静音
- **全屏自动横屏**（`screen.orientation.lock('landscape')`，iOS 不支持）
- **简介折叠**（超过 5 行自动收起，可展开）
- **封面占位图**（视频加载前显示封面，不是黑屏）
- **慢网络提示**：显示"正在加载视频… X% · 还需约 Y 秒"，缓冲时提示"缓冲中…"
- 点赞 / 收藏（本地存储）、分享（复制链接）
- 进度条可拖动，显示已缓冲范围

### 其他
- **缓存版本号机制**（见下方第五节，很重要）
- 404 页面
- 手机 / 平板 / 电脑响应式适配
- 手机性能优化（关掉毛玻璃等高耗效果）

---

## 五、⚠️ 关键机制与踩过的坑

### 1. 缓存版本号机制（非常重要）

**问题**：GitHub Pages 给所有文件设了 `Cache-Control: max-age=600`（10 分钟缓存）。
用户改了网站，访客却看到旧版 —— 因为浏览器缓存了 HTML，而 HTML 里写的是旧版本号。

**解决**：项目有 3 处版本号，**每次改完网站必须同时 +1**：

| 文件 | 位置 |
|---|---|
| `js/data.js` | `SITE_CONFIG.version: '10'` |
| `index.html` | 所有 `?v=10` |
| `watch.html` | 所有 `?v=10` |

原理：首页的作品链接会自动带上版本号（`js/home.js` 里实现），
版本号一变，浏览器就认为是新网址，重新下载全部资源。

**当前版本号：11**

### 2. 手机端手势（踩过坑）

- 触摸设备上**必须屏蔽 mousemove 事件** —— 否则手机点一下会留下"悬停"状态，控制条永远收不回去
- CSS 里的 `:hover` 规则要包在 `@media (hover: hover) and (pointer: fine)` 里
- 暂停状态下允许收起控制条：用 `.paused:not(.pc-hidden)`

### 3. 视频编码（踩过坑）

- 原视频（豆包 AI 生成）是 H.264 720p 24fps，参数一致，可以**无损拼接**（`-f concat -c copy`）
- 拼接后音频时间戳会不连续（`Non-monotonic DTS`），需**重新编码音频**修复：`-c:v copy -c:a aac`
- 必须加 `-movflags +faststart`，否则网页要下完整个文件才能播

### 4. 🐌 GitHub Pages 在国内的速度问题（核心痛点）

**实测数据**（这台机器）：

| 时段 | 速度 | 播放体验 |
|---|---|---|
| 下午（闲时） | 205 KB/秒 ~ 1.1 MB/秒 | 720p 流畅 ✅ |
| 晚上 9 点（高峰） | 24 ~ 51 KB/秒 | 卡 ❌ |

**尝试过但失败的方案**：
- CRF29 压缩（1019 kbps，需要 127 KB/秒）→ 还是卡
- **480p @ 180kbps**（需要 29 KB/秒）→ **既卡又糊，最差结果**

**结论**：压缩解决不了问题，因为瓶颈是**时段性的国际带宽**，不是码率。

**用户最终决定**：**保住 720p 高清画质**（宁可网速好时看），不要压缩。

**如果以后还想彻底解决**，可选方案（用户目前都没选）：
1. **B站嵌入** —— 最快，但播放器会变成 B站样式，且需要投稿审核。用户看过说明后说"那不行"
2. **国内云存储**（七牛云等）—— 能保留自定义播放器，但需实名认证
3. **720p @ 800kbps 折中** —— 体积减半，画质损失小

### 5. bat 文件编码（踩过坑）

`一键发布.bat` 和 `本地预览.bat` **必须是 GBK 编码**（Windows 中文系统原生支持）。
如果用 UTF-8 写，中文会显示成乱码。
（用 Write 工具写完要用 PowerShell 转成 GBK）

### 6. 本机环境

| 工具 | 位置 | 备注 |
|---|---|---|
| **ffmpeg 9.0.1** | `D:\tools\ffmpeg\ffmpeg-9.0.1-full_build\bin\` | 从 GyanD/codexffmpeg 官方下，含 ffmpeg/ffprobe/ffplay |
| **GitHub CLI** | `D:\tools\gh\bin\gh.exe` | 已登录 hi3612（凭据存在 Windows 凭据管理器，重启不丢） |
| **puppeteer-core** | `D:\tools\inspect\` | 用于自动化测试网页（连接本机 Edge） |

**ffmpeg 输出路径要注意**：Windows 路径带 `:` 会被 tar 当成远程主机，用相对路径或 PowerShell 解压。

---

## 六、常用操作

### 加新作品（AI 操作流程）

1. **合成视频**（如果是散片段）：
   - 先复制到英文路径的工作目录（避免中文路径问题）
   - 检查片段参数是否一致：`ffprobe -show_entries stream=codec_name,width,height,r_frame_rate`
   - 写 concat 清单 → 拼接 → 音频重编码修复时间戳 → 加 faststart

2. **生成封面**：`ffmpeg -ss <秒> -i 视频 -frames:v 1 -q:v 2 -vf scale=960:540 封面.jpg`
   （多抽几个时间点，让用户挑）

3. **放进项目**：
   - 视频 → `videos/<作品id>/ep01.mp4`（**文件名用英文**）
   - 封面 → `assets/covers/<作品id>.jpg`

4. **改 `js/data.js`**：往 `WORKS` 数组**最前面**加一段（字段说明见文件内注释）

5. **升版本号 + 发布**（见第五节）

### 发布

```bash
cd "D:/Claude code个人作品网站"
git add -A
git commit -m "中文提交信息"
git push
```

> ⚠️ 网络慢时推送可能超时（HTTP 408）。已配置过：
> `git config http.postBuffer 524288000` / `lowSpeedLimit 1000` / `lowSpeedTime 600`
> 失败了重试即可。

### 测试网页（AI 用）

```bash
cd "D:/tools/inspect"
node check.js     # 检查溢出、报错、元素尺寸
node verify.js    # 验证功能
node test-play.js # 测试播放流畅度
```

用 puppeteer-core 驱动本机 Edge（headless）。
⚠️ 注意：headless 环境的视频播放性能**不代表真实体验**，别被卡顿数据误导。

---

## 七、当前作品列表

### 《报告，校草总想社死》（id: `xiaocao`）—— 已上线 3 集

**这是 40 集规划的长篇作品**，分镜脚本在
`D:\3D动画《报告，校草总想社死》\《校草总想社死》.txt`（53KB，含每集分镜、台词、运镜）。

**角色**：顾言澈（男主，高冷校草）、林小满（女主）、死党二人组
**设定**：一个「系统」绑定男主，每天发布"社死任务"

| 集数 | 官方标题 | 时长 | 素材来源 |
|---|---|---|---|
| 第 1 集 | 食堂社死・系统降临 | 1:30 | 原来叫 01~09.mp4，现叫 1-1~1-9.mp4 |
| 第 2 集 | 图书馆社死・小苹果 | 1:30 | 原来叫 10~18.mp4，现叫 2-1~2-9.mp4 |
| 第 3 集 | 体育课社死・海贼王 | 1:36 | 3-1~3-9.mp4 |

> ⚠️ **集数标题必须用脚本里的官方标题**，不要自己编（我第一次自己起的名"躲不掉的人"就不对）。

**用户手上还有素材没上架**（都在 `D:\` 下）：

| 文件夹 | 内容 | 备注 |
|---|---|---|
| `2D动画短片《蓝箱》` | 15 个散段（1-1 ~ 5-3），共 2:46 | 需合成 |
| `2D动画短片《虹之底》` | 完整版 1:40 + 10 个散段 | 有完整版 |
| `2D动画短片《雨季不再来》` | 完整版 2:01 + 12 个散段 | 有完整版 |
| `3D动画短片` | 合成1 (3:37) + 合成2 (2:30) + 6 个散段 | 有完整版 |

用户说过"先等等"再上这些。

> 📌 **40 集的隐患**：选集列表现在是「每行一集」的竖排列表，
> 集数多了（比如 20 集以上）会很长，手机上要滚很久。
> 到时候建议改成**网格布局**（像 B站 那样每行 4-5 个小方块）。

---

## 八、用户的偏好与沟通方式

1. **全程中文**（用户英文不好，工具 description 也要中文）
2. **改系统前要说明**（比如装软件、改配置，先说清楚做什么、会不会改系统）
3. **不要占 C 盘**（用户明确要求过，工具装到 `D:\tools`）
4. **画质优先于流畅度**（用户最终选择了 720p 而不是压缩版）
5. **用户会自己测试并给反馈**（他很配合，反馈也具体）
6. 用户喜欢**用手机测试**，也会用电脑

---

## 九、TODO / 待办

- [x] ~~网速问题~~ → **2026-09-16 实测：白天看流畅，几乎无卡顿**。
      结论：**保持 720p 高清**，网络好的时段完全够用，不需要压缩
- [ ] **继续做后续集数**：用户会陆续生成片段，命名规律 `4-1`~`4-9`、`5-1`~`5-9`……
      流程：复制到英文工作目录 → concat 拼接 → 音频重编码修复时间戳 → faststart
      → 放进 `videos/xiaocao/epNN.mp4` → 改 `data.js` → 升版本号 → 推送
      **标题要去脚本 `《校草总想社死》.txt` 里找官方标题**
- [ ] 其他 4 部作品待上架（用户说"先等等"）
- [ ] 评论区（Giscus）已内置代码但未配置 —— 用户说"就我自己看"，暂时不需要
- [ ] 集数多了以后（>15 集），选集列表考虑改成网格布局
